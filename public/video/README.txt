Drop your hero background video here.

Expected filenames:
  hero.mp4   (primary - widely supported)
  hero.webm  (optional fallback)

Recommendations:
- 1920x1080 or 1280x720
- Max 8 seconds, looping
- Under 3 MB
- Muted, no audio track needed
- Subtle motion (people, hands, city, abstract). Avoid fast cuts.

The hero will gracefully fall back to the teal gradient if no file is present.
